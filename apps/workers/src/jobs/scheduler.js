/**
 * Scheduled Jobs Registry
 * All cron jobs MUST be registered here.
 */
export async function startScheduler() {
  console.log('Scheduler initialized: Registered jobs: [preScaleForEvent, passwordExpiryCheck]');
}
